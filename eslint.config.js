/** @type {import('eslint').Linter.Config} */
const config = {
    rules: {
      'space-infix-ops': ['error'],
      // Добавьте другие правила по мере необходимости
    },
    // Другие настройки конфигурации (например, env, parserOptions и т.д.)
  };
  
  module.exports = config;
  